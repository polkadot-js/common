name: Nightly ss58-registry check
on:
  schedule:
    - cron:  '1 5 * * *'

jobs:
  alltests:
    strategy:
      matrix:
        step: ['test:networks']
    name: ${{ matrix.step }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: ${{ matrix.step }}
      run: |
        yarn install --immutable | grep -v 'YN0013'
        yarn ${{ matrix.step }}
