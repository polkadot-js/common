{
  "version": "0.13.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel && (cd packages && (cd util && polkadot-dev-build-docs && cd ..) && (cd util-crypto && polkadot-dev-build-docs && cd ..) && cd ..)",
    "check": "eslint packages && flow check",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.14.10",
    "lerna": "^2.5.1"
  }
}
