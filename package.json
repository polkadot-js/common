{
  "version": "0.19.6",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel && polkadot-dev-build-docs",
    "check": "eslint packages && flow check",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.18.1",
    "lerna": "^2.5.1"
  }
}
