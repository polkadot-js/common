{
  "version": "0.18.6",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel && polkadot-dev-build-docs",
    "check": "eslint packages && flow check",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.17.6",
    "lerna": "^2.5.1"
  }
}
