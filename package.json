{
  "version": "0.22.7",
  "private": true,
  "engines": {
    "node": "^10.1.0",
    "yarn": "^1.7.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel && polkadot-dev-build-docs",
    "check": "eslint packages && flow check",
    "clean": "polkadot-dev-clean-build",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.19.23"
  }
}
